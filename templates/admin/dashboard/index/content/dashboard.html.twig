{# A hookable receives nothing but `hookable_metadata`: the page's variables travel in its context. #}
{% include '@DurablePlugin/admin/dashboard/_dashboard.html.twig' with hookable_metadata.context.all only %}
